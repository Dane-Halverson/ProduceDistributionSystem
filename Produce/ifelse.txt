else if ( type == Corn || type == corn ) {
    item = std::make_shared<Corn>(n, date, daysSincePick, nameOfFarm);
}
else if ( type == GreenPepper || type == greenpepper ) {
    item = std::make_shared<GreenPepper>(n, date, daysSincePick, nameOfFarm);
}
else if ( type == Lettuce || type == lettuce ) {
    item = std::make_shared<Lettuce>(n, date, daysSincePick, nameOfFarm);
}
else if ( type == Carrot || type == carrot ) {
    item = std::make_shared<Carrot>(n, date, daysSincePick, nameOfFarm);
}
else if ( type == Apple || type == apple ) {
    item = std::make_shared<Apple>(n, date, daysSincePick, nameOfFarm);
}
else if ( type == Onion || type == onion ) {
    item = std::make_shared<Onion>(n, date, daysSincePick, nameOfFarm);
}
else if ( type == Squash || type == squash ) {
    item = std::make_shared<Squash>(n, date, daysSincePick, nameOfFarm);
}
else if ( type == Avocados || type == avocados ) {
    item = std::make_shared<Avocados>(n, date, daysSincePick, nameOfFarm);
}
